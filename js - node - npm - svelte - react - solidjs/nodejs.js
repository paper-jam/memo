 // récupération des arguments en ligne de commande
var Tkeys = process.argv


global.console.log()  // global est optionnel
// les modules
console.log(module)



// tampon de sortie
process.stdout.write('coucou');
process.on('exit', function () ...


// modul util
var util = require('util');
var message = util.format('%s : %s', 'Le dossier est', myPath);

// cond. ternaire
condition ? exprSiVrai : exprSiFaux
age > 18 ? location.assign("continue.html") : stop = true;

// exec
var exec = require('child_process').exec;
exec('node -v', function(error, stdout) {...

// file system : les commandes
fs.appendFile
fs.readdir
fs.readFile
fs.mkdir
fs.rmdirSync
fs.createReadStream // pour les gros fichiers

// HTTP
    var responseBody = `
        <html>
            <head></head>
            <body>
            <h1>Je suis un serveur HTTP</h1>
            <p>${req.url}</p>
            <p>${req.method}</p>
            </body>
        </html>
    `;

// librairies intéressantes
// date-fns         mieux que moment.js
// dotenv           permet de définir des varaibales d'environnements dans le projet. It a zero-dependency.
// socket.io        
// uuid             generation unique identifier
// axios 








